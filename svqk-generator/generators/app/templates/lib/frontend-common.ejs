<%# args: (entityNmPascal: string, compIdFields?: Field[]) %>
<%
idType =  compIdFields ? entityNmPascal + "IdModel" : (idField.javaType === "Integer" ? "number" : "string");

importingIdType = compIdFields ? ", " + idType : "";

importDecIdType = compIdFields ? `import type { ${idType} } from "$lib/arch/api/Api";` : "";

importDecIdTypeE2etest = compIdFields ? `import type { ${idType} } from "@api/Api";` : "";

idObj = compIdFields ? `id: {} as ${entityNmPascal}IdModel` : "";
%>
