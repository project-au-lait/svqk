<%# args: fields %>

<% include('java-type-def'); %>
<%
getValueCode = (field) => findTypeDef(field.javaType)?.valueCode ?? "null";

imports = [...new Set(fields.map((field) => field.javaType))].reduce(
  (result, type) => result.concat(findTypeDef(type)?.imports ?? []),
  []
);

idField = fields.find((field) => field.id);
idFieldNm = idField?.fieldName ?? "";
%>